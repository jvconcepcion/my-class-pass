export enum PrivacyStatus { // necessary as wix declare privacyStatus as enum
  PUBLIC = 'PUBLIC',
  PRIVATE = 'PRIVATE'
};

export enum SeverityStatus {
  INFO = 'info',
  SUCCESS = 'success',
  WARNING = 'warning',
  ERROR = 'error'
};